/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'http',
        hostname: 'localhost',
        port: '3845',
        pathname: '/assets/**',
      },
      {
        protocol: 'https',
        hostname: 'www.caranddriver.com',
        pathname: '/_assets/**',
      },
    ],
  },
}

module.exports = nextConfig

